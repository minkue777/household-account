# 지출 통계 로딩 지연 조사 — 2026-09-07

지출 통계가 느려질 수 있는 코드 변경을 확인했으며, 같은 운영 자료를 읽는 비교에서도 지연 증가를 재현했습니다. 정상 배포 `448f8d0`은 날짜 범위 하나를 실시간 구독했고, 현재 배포 `869a064`는 같은 범위를 50건씩 순차 조회하여 마지막 페이지까지 도착한 뒤 화면에 표시합니다. 이 전환은 `2de469a`(요구사항 전수 감사 수정 및 v1.2.22 배포 준비)에서 도입됐습니다.

사용자 기기의 실제 화면 표시 시간을 측정한 것은 아닙니다. 아래 운영 측정은 이 PC에서 Admin SDK로 수행한 읽기 전용 비교이며, 코드 경로의 차이와 별도의 통제 실험을 함께 근거로 사용했습니다. 이 측정에는 지출 통계 코드 변경이나 성능 개선을 포함하지 않았습니다.

기본 선택 기간은 이전·현재 코드 모두 **1년**입니다. 조사 시점의 기본 날짜 범위는 `2025-10-01`부터 `2026-09-30`까지이며, 해당 가구에서 쿼리에 일치하는 전체 문서는 **1,742건**, 삭제·대체된 문서와 수입 등을 제외한 표시 대상 지출은 **1,645건**이었습니다. 페이지 수는 표시 대상 지출 수가 아니라 필터 적용 전 전체 문서 수로 결정됩니다.

| 선택 기간 | 조회 날짜 범위 | 전체 문서 수 | 이전 방식 | 현재 순차 조회 수 |
| --- | --- | ---: | ---: | ---: |
| 3개월 | 2026-07-01 ~ 2026-09-30 | 619 | 구독 1개 | 13회 |
| 6개월 | 2026-04-01 ~ 2026-09-30 | 1,264 | 구독 1개 | 26회 |
| 1년 · 기본값 | 2025-10-01 ~ 2026-09-30 | 1,742 | 구독 1개 | 35회 |

현재 구현은 페이지 길이가 50보다 작아야 완료하므로, 전체 문서 수가 `N`이면 서버 조회 횟수는 `floor(N / 50) + 1`입니다. 정확히 50의 배수인 경우에도 마지막 빈 페이지를 한 번 더 확인합니다. 각 다음 페이지는 직전 요청 완료와 cursor 확보를 기다립니다. 이전의 구독 1개가 물리적인 네트워크 패킷 1개라는 뜻은 아닙니다.

운영 비교는 기본 1년 범위에 대해 연결 예열용 단일 조회를 한 번 수행한 뒤, 단일 조회와 50건 순차 조회를 번갈아 두 번씩 실행했습니다. Admin SDK의 단일 `get()`은 이전 브라우저 구독의 조회 범위를 비교하기 위한 대리 방식입니다. 두 방식 모두 전체 문서를 읽었으며, 필터와 총액 계산은 동일하게 적용했습니다.

| 비교 회차 | 단일 조회 | 50건 순차 조회 | 요청 수 |
| --- | ---: | ---: | --- |
| 1 | 1,306ms | 2,683ms | 1회 / 35회 |
| 2 | 1,236ms | 2,600ms | 1회 / 35회 |
| 평균 | **1,271ms** | **2,641.5ms** | 현재가 **약 2.08배** |

예열 조회는 1,230ms였습니다. 모든 측정에서 전체 1,742문서, 표시 대상 지출 1,645건이 일치했으며 지출 행의 ID·금액·날짜·카테고리와 총액의 일치 여부를 내부에서 확인했습니다. 해당 값 자체는 결과에 기록하지 않았습니다. 예열을 포함한 벤치마크 문서 반환량은 8,710건이며, 기간별 건수 확인에는 별도의 집계 조회 3회를 사용했습니다. 측정 결과는 2026-09-07 18:02 KST에 기록했습니다.

통제 실험에서는 두 커밋의 실제 조회 코드, 문서 매퍼, 표시 여부 판단 코드, 이전의 메모리 보존 코드를 불러왔습니다. 운영 데이터 대신 합성 문서를 사용하고 Firebase SDK 요청마다 동일한 **100ms 지연을 인위적으로 부여**했습니다. SDK와 사용하지 않는 명령 경계만 대체했으며, 양쪽 결과의 행 수와 총액이 같음을 확인했습니다.

| 합성 전체 문서 수 | 이전 완료 시간 · 요청 수 | 현재 완료 시간 · 요청 수 |
| ---: | ---: | ---: |
| 50 | 113ms · 1회 | 217ms · 2회 |
| 116 | 101ms · 1회 | 332ms · 3회 |
| 1,034 | 101ms · 1회 | 2,274ms · 21회 |
| 1,645 | 105ms · 1회 | 3,555ms · 33회 |

이 실험에서 현재 코드의 최대 동시 조회 수는 1이었습니다. 100ms는 실제 운영 요청 지연을 측정한 값이 아니며, 표의 시간이나 배율을 사용자 기기의 체감 시간으로 해석하면 안 됩니다. 운영 비교와 달리 문서 전송량, 실제 Firebase 전송 방식, 인증, 서버 처리, React·차트 렌더링, 브라우저 캐시는 포함하지 않았습니다.

재진입 동작에도 차이가 있습니다. 이전 구현은 같은 세션·같은 기간의 마지막 결과를 `retainedSnapshots`에 보관하고 다시 구독할 때 즉시 표시한 뒤 서버 구독을 이어갑니다. 현재 구현은 진입하거나 조회 조건이 바뀔 때 `setExpenses([])`로 화면 자료를 지우고 서버 페이지를 처음부터 다시 읽습니다. 동일 합성 자료 1,645건으로 같은 런타임에서 재진입했을 때 이전의 보관 결과 표시는 1ms, 현재의 재조회 완료는 3,559ms·33회였습니다. 이는 이전 세션 메모리에 보관 결과가 있는 경우의 실험이며, 브라우저를 완전히 종료한 뒤에도 해당 값이 유지된다는 의미는 아닙니다.

근거가 되는 코드 위치는 다음과 같습니다. 줄 번호는 각 커밋 기준입니다.

| 커밋 | 파일 · 줄 | 확인 내용 |
| --- | --- | --- |
| `448f8d0` | `web/src/app/stats/page.tsx:26`, `:115` | 기본 1년, 기간 구독 callback에서 화면 자료 반영 |
| `448f8d0` | `web/src/lib/expenseService.ts:571`, `:589`, `:596` | 날짜 범위 query 하나와 `onSnapshot` 구독 |
| `448f8d0` | `web/src/platform/read-model/optimisticEntityProjection.ts:40`, `:53`, `:63`, `:70` | 직전 결과 보관 및 재구독 시 즉시 발행 |
| `869a064` | [stats/page.tsx](../../web/src/app/stats/page.tsx) `:23`, `:82`, `:88`, `:90` | 기본 1년, 자료 초기화, 전체 조회 완료 후 표시, 재조회 조건 |
| `869a064` | [expenseStatisticsReadModel.ts](../../web/src/platform/reporting/expenseStatisticsReadModel.ts) `:20`, `:23`, `:25`, `:41` | 직렬 `await`, 50건 제한, cursor, 마지막 페이지 뒤 반환 |

최소 개선 후보는 현재의 날짜 범위·cursor·세션 보호·전체 조회 완료 후 발행 규칙을 유지하면서 페이지 크기를 조정하는 것입니다. 예를 들어 500건으로 늘리면 현재 운영 범위 1,742건은 **35회에서 4회로 감소할 것으로 계산됩니다**. 이는 요청 수 예측이며, 해당 변경을 구현하거나 변경 후 시간을 측정하지 않았습니다. 재진입 시 직전 성공 자료를 재사용하는 개선은 세션 격리와 새로고침 표시를 포함하여 별도로 검토해야 합니다.

원시 결과는 로컬 임시 디렉터리의 다음 파일에 보관했습니다. 저장소 보고서와 결과 JSON에는 사용자 이름·가구 ID·상호·실제 금액을 포함하지 않았습니다.

- `%TEMP%/household-expense-statistics-production-benchmark-20260907.json`: 운영 비교 결과
- `%TEMP%/household-expense-statistics-production-benchmark-20260907.cjs`: 읽기 전용 운영 비교 스크립트
- `%TEMP%/household-expense-statistics-controlled-benchmark-20260907.json`: 실제 코드 통제 실험 결과

이 측정에서 **운영 데이터 쓰기 0회, 알림 발송 0회**였습니다. 확인된 결론은 이번 조회 방식 변경이 로딩 지연을 늘리는 구조이며 운영 자료 비교에서도 차이가 재현됐다는 것입니다. 사용자 기기에서 정확히 몇 초가 늘었는지는 별도의 실제 화면 측정이 필요합니다.

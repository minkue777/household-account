interface:
  display_name: "가계부 개발·배포·CI 후속 확인"
  short_description: "변경 대상만 배포하고 CI 결과는 후속 확인으로 처리합니다."
  default_prompt: "$household-delivery 스킬로 요청한 변경을 구현·push하고 필요한 대상만 배포한 뒤, 미완료 CI는 후속 확인으로 넘겨 주세요."
policy:
  allow_implicit_invocation: true
